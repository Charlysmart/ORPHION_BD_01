# ORPHION_BD_01
To-Do List API: A simple FastAPI-based API for managing tasks in-memory. It supports creating, retrieving, updating, and deleting tasks, with duplicate checks and no persistent storage.
